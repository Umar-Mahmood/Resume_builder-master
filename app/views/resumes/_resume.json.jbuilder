json.extract! resume, :id, :first_name, :last_name, :last_name, :discription, :serial_number, :phone, :adress, :email, :designation, :skills, :created_at, :updated_at
json.url resume_url(resume, format: :json)
